pip install scikit-learn
from sklearn.neighbors import KNeighborsClassifier

# Пример набора данных: размеры одежды и метки классов
# Предположим, что у нас есть два признака (длина и ширина) и три класса ("футболка", "брюки", "платье")
# Ваш реальный набор данных будет содержать больше примеров и признаков.
X_train = [[50, 30], [40, 25], [60, 35], [55, 33], [45, 28], [70, 40]]
y_train = ['футболка', 'футболка', 'брюки', 'платье', 'футболка', 'платье']

# Создаем модель k-Nearest Neighbors с числом соседей равным 3
model = KNeighborsClassifier(n_neighbors=3)

# Обучаем модель на тренировочных данных
model.fit(X_train, y_train)

# Пример новой одежды, которую мы хотим классифицировать
new_clothing = [[48, 32]]

# Прогнозируем класс новой одежды
predicted_class = model.predict(new_clothing)

# Выводим предсказанный класс
print("Предсказанный класс для новой одежды:", predicted_class)
